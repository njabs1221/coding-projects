# Refined Code Notes

These files were cleaned up after the first classification pass.

| Refined file | What was fixed |
|---|---|
| `math-and-number-theory/leap_year_checker.cpp` | Corrected the leap-year condition and added a small helper function. |
| `math-and-number-theory/polynomial_formula.py` | Replaced bitwise XOR with exponentiation. |
| `lists-and-statistics/second_largest_until_minus_one.py` | Fixed sentinel handling and added a safe message for short input. |
| `word-game/wordle_feedback.py` | Rebuilt the feedback logic with clear two-pass matching. |
| `math-and-number-theory/gcd_calculator.cpp` | Initialised the result properly by replacing the loop with Euclid's algorithm. |
| `matrices-and-linear-algebra/matrix_vector_multiply.py` | Rebuilt input handling and matrix-vector multiplication. |
| `word-game/player_guess_strategy.py` | Replaced the stub with a simple valid next-word strategy. |
| `math-and-number-theory/schmerg_nested_formula.py` | Moved unreachable code into executable top-level logic. |
| `strings-and-text/uppercase_lines.cpp` | Fixed the off-by-one loop and handles full input lines. |

The earlier `needs-fix/` copies were removed because fixed versions now exist in the correct categories.
